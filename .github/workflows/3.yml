name: workflow_dispatch_3

on: 
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to deploy to"
        required: true
        type: choice
        options:
          - staging
          - production
      debug:
        description: "Enable debug mode"
        required: true
        type: boolean
        default: false
  push:
    paths:
      - "src/**"
      - "**/*.js"
      - '!**/*.md'
    # paths-ignore:
    #   - "docs/**"
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: "0 0 * * 0"
    
jobs:
  lint:
    runs-on: ubuntu-latest
    continue-on-error: true  # Job continues even if tests fail
    steps:
      - run: |
          echo "Linting..."
          exit 1
  
  build:
    needs: lint
    timeout-minutes: 30  # Job fails if it runs longer than 30 minutes
    runs-on: ubuntu-latest
    steps:
      - run: echo "Building..."
        timeout-minutes: 10  # Step fails if it runs longer than 10 minutes
  
  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: echo "Testing..."
  
  deploy:
    runs-on: ubuntu-latest
    needs: 
      - test
      - build
    steps:
      - name: Deploy to ${{ inputs.environment }}
        run: echo "Deploying to ${{ inputs.environment }}"
